import wind from "../assets/svg/wind.svg?raw"; // raw 提取详细信息、url 获取本地地址

document.body.innerHTML = wind;

// const wind_svg = document.getElementsByTagName("svg")[0];

// wind_svg.onmouseenter = function () {
//   this.style.fill = bule;
// };

console.log("svg: ----------------------- ", wind);

// 在使用 svg 时，依旧可以像使用图片的方式将其在页面上展示
